# Configuration Azure AD
# Obtenez ces valeurs depuis le portail Azure (voir AZURE_AD_CONFIG.md)

# Application (client) ID
AZURE_AD_CLIENT_ID=xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx

# Client Secret (créé dans "Certificates & secrets")
AZURE_AD_CLIENT_SECRET=xxx~xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Directory (tenant) ID
AZURE_AD_TENANT_ID=xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx

# URL de l'application
APP_URL=http://localhost:3000

# URL de redirection après login (doit correspondre à celle configurée dans Azure)
REDIRECT_URI=http://localhost:3000/auth/redirect

# Secret de session (générez une chaîne aléatoire longue)
# Utilisez: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SESSION_SECRET=changez-moi-par-un-secret-tres-long-et-aleatoire

# Port du serveur (optionnel, défaut: 3000)
PORT=3000

# Environnement (development ou production)
NODE_ENV=development
