# Configuration de l'application de gestion de stock
# Copiez ce fichier vers .env et modifiez les valeurs

# Port du serveur
PORT=3000

# Environnement (development, production)
NODE_ENV=development

# Secret pour les sessions (IMPORTANT: Générez une chaîne aléatoire longue et unique)
# Vous pouvez en générer un avec: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SESSION_SECRET=change-this-to-a-very-long-random-string-in-production

# Origine autorisée pour CORS (en production, mettez votre domaine)
ALLOWED_ORIGIN=http://localhost:3000

# Configuration email (optionnel - pour notifications futures)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-app-password
# SMTP_FROM=stock@example.com
